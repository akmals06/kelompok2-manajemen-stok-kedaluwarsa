{
  "name": "kelompok2-manajemen-stok-kedaluwarsa",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo - Sistem Manajemen Stok & Kedaluwarsa UMKM",
  "scripts": {
    "dev": "concurrently --shell powershell -k -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "prisma:generate": "npm run prisma:generate --prefix backend",
    "prisma:migrate": "npm run prisma:migrate --prefix backend",
    "prisma:studio": "npm run prisma:studio --prefix backend",
    "prisma:seed": "npm run prisma:seed --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "cors": "^2.8.6"
  }
}
