import LoginClient from './LoginClient';

export const metadata = {
  title: 'Masuk — Stok Kedaluwarsa UMKM',
  description: 'Login ke Sistem Manajemen Stok dan Kedaluwarsa Warung Sembako Abah Andi.',
};

export default function LoginPage() {
  return <LoginClient />;
}
